{
  "name": "@outlive/core",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "The custody model, the analysis engine, the input guard and the document generators. No React, no DOM, no I/O.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "typescript": "^5.9.3",
    "vitest": "^5.0.0",
    "vite": "^8.2.2"
  }
}
